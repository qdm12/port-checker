version: '3'
services:
  port-checker:
    build: .
    image: qmcgaw/port-checker:latest
    container_name: port-checker
    restart: always
    #network_mode: host
    #environment:
    #  - PORT=1234
    network_mode: bridge
    ports:
      - 8000:8000/tcp
